package functions

// figure this out
// func NOT(term terminal.Terminal, arguments []variable.User_variable) (variable.User_variable, error) {
// 	if len(arguments) != 1 {
// 		return nil, errors.New("NOT: Invalid number of arguments")
// 	}
// 	if arguments[0].Type_of() != variable.NUMBER {
// 		return nil, errors.New("NOT: Invalid type")
// 	}
// 	a := arguments[0].Value().(float64)

// 	res := !a

// 	return variable.VARTYPE_BOOLEAN{}.New(res), nil
// }
